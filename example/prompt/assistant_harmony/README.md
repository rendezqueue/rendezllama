# Harmony Assistant

This example should be run with [Harmony](https://github.com/openai/harmony)-style models that are tuned to behave like an instruction-following assistant chatbot.

The model typically should have special `<|start|>`, `<|end|>`, `<|channel|>`, `<|return|>`, and other tokens.
