
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/cmake_module")


# Reintroduce NDEBUG so libraries get the flags their expected defaults.
set(CMAKE_C_FLAGS_RELEASE "${CMAKE_C_FLAGS_RELEASE} -DNDEBUG")
set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -DNDEBUG")
set(CMAKE_C_FLAGS_RELONHOST "${CMAKE_C_FLAGS_RELONHOST} -DNDEBUG")
set(CMAKE_CXX_FLAGS_RELONHOST "${CMAKE_CXX_FLAGS_RELONHOST} -DNDEBUG")
if (MSVC)
  string(REPLACE "-DNDEBUG" "/DNDEBUG" CMAKE_C_FLAGS_RELEASE "${CMAKE_C_FLAGS_RELEASE}")
  string(REPLACE "-DNDEBUG" "/DNDEBUG" CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE}")
  string(REPLACE "-DNDEBUG" "/DNDEBUG" CMAKE_C_FLAGS_RELONHOST "${CMAKE_C_FLAGS_RELONHOST}")
  string(REPLACE "-DNDEBUG" "/DNDEBUG" CMAKE_CXX_FLAGS_RELONHOST "${CMAKE_CXX_FLAGS_RELONHOST}")
endif()


include(FetchContent)
include("cmake_fetchcontent/fildesh.cmake")
include("cmake_fetchcontent/llama_cpp.cmake")
